/**
 * Value type for chip-count-select form control.
 */
export interface ChipCountItem {
  id: number;
  count: number;
}

/**
 * Option displayed in the dropdown (available items to add).
 */
export interface ChipCountOption {
  id: number;
  label: string;
}
